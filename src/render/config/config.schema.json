{
  "namespace": "music_lyric_player::render::config",
  "file": "config.gen.h",
  "imports": [
    { "as": "container", "schema": "container/index.schema.json" },
    { "as": "layout", "schema": "layout/index.schema.json" },
    { "as": "scroll", "schema": "scroll/index.schema.json" },
    { "as": "line", "schema": "line/index.schema.json" }
  ],
  "items": [
    {
      "name": "Root",
      "desc": "Renderer configuration.",
      "fields": [
        {
          "name": "container",
          "nested": "container.Root",
          "desc": [
            "Background fill and padding of the player."
          ]
        },
        {
          "name": "layout",
          "nested": "layout.Root",
          "desc": [
            "Alignment and spacing between lyric lines."
          ]
        },
        {
          "name": "scroll",
          "nested": "scroll.Root",
          "desc": [
            "Viewport scrolling and its transition animation."
          ]
        },
        {
          "name": "line",
          "nested": "line.Root",
          "desc": [
            "Font and per-state colors of lyric lines."
          ]
        }
      ]
    }
  ]
}
