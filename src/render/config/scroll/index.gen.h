// clang-format off
// Generated by script/generate-config.py from index.schema.json. DO NOT EDIT.
// To change config, edit the schema and regenerate with script/generate-config.py.
#ifndef MUSIC_LYRIC_PLAYER_RENDER_CONFIG_SCROLL_CONFIG_GEN_H_
#define MUSIC_LYRIC_PLAYER_RENDER_CONFIG_SCROLL_CONFIG_GEN_H_

#include <cstddef>
#include <optional>
#include <string>
#include <string_view>

#include "utils/manager/config.h"

namespace music_lyric_player::render::config::scroll {
	/**
	 * Transition animation used when the active line changes.
	 */
	struct AnimationConfig {
		/**
		 * Transition duration when the active line changes, in `ms`.
		 * `0` snaps to the target without easing.
		 *
		 * @default 450
		 */
		double duration = 450.0;
	};

	/**
	 * Viewport scrolling behaviour driven by the active line.
	 */
	struct Root {
		/**
		 * Vertical anchor of the active line inside the viewport,
		 * as a ratio (`0`–`1`) of its height.
		 *
		 * - `0.5` — keep the active line at the vertical center
		 * - `0.3` — keep the active line closer to the top
		 *
		 * @default 0.5
		 */
		double anchor = 0.5;
		/**
		 * Transition animation used when the active line changes.
		 */
		AnimationConfig animation;
	};

	struct AnimationConfigPatch {
		::std::optional<double> duration;
	};

	struct RootPatch {
		::std::optional<double> anchor;
		AnimationConfigPatch animation;
	};

	/**
	 * Deep-merges a sparse patch into a config.
	 */
	inline void applyConfigPatch(AnimationConfig& cfg, const AnimationConfigPatch& patch) {
		if (patch.duration.has_value()) {
			cfg.duration = *patch.duration;
		}
	}

	/**
	 * Deep-merges a sparse patch into a config.
	 */
	inline void applyConfigPatch(Root& cfg, const RootPatch& patch) {
		if (patch.anchor.has_value()) {
			cfg.anchor = *patch.anchor;
		}
		applyConfigPatch(cfg.animation, patch.animation);
	}

	/**
	 * Diffs two configs into a dot-path key set; a changed child also records its parent path.
	 */
	inline void diffConfig(const AnimationConfig& prev, const AnimationConfig& next, const ::std::string& prefix, ::music_lyric_player::utils::config::ChangeKeys& keys) {
		if (prev.duration != next.duration) {
			keys.insert(prefix + "duration");
		}
	}

	/**
	 * Diffs two configs into a dot-path key set; a changed child also records its parent path.
	 */
	inline void diffConfig(const Root& prev, const Root& next, const ::std::string& prefix, ::music_lyric_player::utils::config::ChangeKeys& keys) {
		if (prev.anchor != next.anchor) {
			keys.insert(prefix + "anchor");
		}
		{
			const ::std::size_t before = keys.size();
			diffConfig(prev.animation, next.animation, prefix + "animation" + ".", keys);
			if (keys.size() > before) {
				keys.insert(prefix + "animation");
			}
		}
	}

	namespace AnimationConfigKeys {
		inline constexpr ::std::string_view duration{"duration"};
	} // namespace AnimationConfigKeys

	namespace RootKeys {
		inline constexpr ::std::string_view anchor{"anchor"};
		namespace animation {
			inline constexpr ::std::string_view duration{"animation.duration"};
		} // namespace animation
	} // namespace RootKeys

} // namespace music_lyric_player::render::config::scroll

#endif // MUSIC_LYRIC_PLAYER_RENDER_CONFIG_SCROLL_CONFIG_GEN_H_
// clang-format on
