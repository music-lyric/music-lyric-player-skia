# Mirrors VERSION.txt at the repo root; script/release.py rewrites both in the same release commit.
VERSION_NAME=0.2.0

org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true

android.useAndroidX=true
