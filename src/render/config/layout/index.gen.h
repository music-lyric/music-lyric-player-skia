// clang-format off
// Generated by script/generate-config.py from index.schema.json. DO NOT EDIT.
// To change config, edit the schema and regenerate with script/generate-config.py.
#ifndef MUSIC_LYRIC_PLAYER_RENDER_CONFIG_LAYOUT_CONFIG_GEN_H_
#define MUSIC_LYRIC_PLAYER_RENDER_CONFIG_LAYOUT_CONFIG_GEN_H_

#include <cstddef>
#include <optional>
#include <string>
#include <string_view>

#include "utils/manager/config.h"

namespace music_lyric_player::render::config::layout {
	/**
	 * Horizontal alignment of a line within the container.
	 */
	enum class Align {
		Left,
		Center,
		Right,
	};

	/**
	 * Alignment and spacing applied to every lyric line.
	 */
	struct Root {
		/**
		 * Horizontal alignment of lines within the container.
		 *
		 * @default Align::Left
		 */
		Align align = Align::Left;
		/**
		 * Vertical spacing between adjacent lines.
		 * A `%` value is relative to the viewport height.
		 * A bare number or `px` is an absolute length in logical pixels.
		 *
		 * @default "16px"
		 * @example 16
		 * @example "16px"
		 * @example "2%"
		 */
		::std::string gap = "16px";
	};

	struct RootPatch {
		::std::optional<Align> align;
		::std::optional<::std::string> gap;
	};

	/**
	 * Deep-merges a sparse patch into a config.
	 */
	inline void applyConfigPatch(Root& cfg, const RootPatch& patch) {
		if (patch.align.has_value()) {
			cfg.align = *patch.align;
		}
		if (patch.gap.has_value()) {
			cfg.gap = *patch.gap;
		}
	}

	/**
	 * Diffs two configs into a dot-path key set; a changed child also records its parent path.
	 */
	inline void diffConfig(const Root& prev, const Root& next, const ::std::string& prefix, ::music_lyric_player::utils::config::ChangeKeys& keys) {
		if (prev.align != next.align) {
			keys.insert(prefix + "align");
		}
		if (prev.gap != next.gap) {
			keys.insert(prefix + "gap");
		}
	}

	namespace RootKeys {
		inline constexpr ::std::string_view align{"align"};
		inline constexpr ::std::string_view gap{"gap"};
	} // namespace RootKeys

	/**
	 * Built-in defaults (every field at its schema default); the fallback when a config value fails to parse.
	 */
	inline const Root Default{};

} // namespace music_lyric_player::render::config::layout

#endif // MUSIC_LYRIC_PLAYER_RENDER_CONFIG_LAYOUT_CONFIG_GEN_H_
// clang-format on
